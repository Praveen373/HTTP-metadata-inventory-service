# Copy this file to .env before running locally
# docker-compose uses these values as defaults

MONGO_URI=mongodb://mongo:27017
MONGO_DB_NAME=metadata_inventory
MONGO_COLLECTION_NAME=metadata
HTTP_TIMEOUT_SECONDS=15.0
HTTP_MAX_RETRIES=2
APP_ENV=development
LOG_LEVEL=INFO
